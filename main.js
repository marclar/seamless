/**
 * main.js is responsible for reading the contents of the "models" directory and creating
 *  models for sharing and syncing between the client and server
 */
function main(){}

/**
 * This method will search the given directory for models
 * @param directory
 */
main.prototype.generate = function(directory){
    //TODO
};

module.exports = new main();
